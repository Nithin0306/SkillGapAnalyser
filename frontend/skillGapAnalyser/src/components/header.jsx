import Navbar from "../components/navbar";
import "../App.css";

const Features = () => {
  // Changed to just return the logo without the skill gap analyzer text
  // since it's already in the navbar
  return null;
};

export default Features;